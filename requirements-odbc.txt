# ODBC バックエンド(Access / Oracle)を使う場合のみ追加インストール:
#   pip install -r requirements.txt -r requirements-odbc.txt
#
# 別途、OS 側に ODBC ドライバが必要:
#   - Access : Microsoft Access Database Engine（Access Driver *.mdb, *.accdb）
#              Python の bit 数（64bit 推奨）とドライバの bit 数を一致させること。
#   - Oracle : Oracle ODBC ドライバ / Instant Client、および unixODBC(非Windows)。
pyodbc==5.2.0
